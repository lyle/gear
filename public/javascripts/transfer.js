

function assign_transfer_to(type, id, name){
	// this function updates the transfer display text depending on type
	// type=location ... "to location: name"
	// sets the transfer_to_type type in the transfer form
	// and sets the person_id or location_id to the id passed to this function
	alert(name);
	
}